server:
  port: 8080

spring:
  application:
    name: file-manage-system

  # MySQL Database Configuration
  datasource:
    url: jdbc:mysql://localhost:3306/filemanage_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: qwe123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  # Mail Configuration (QQ邮箱SMTP)
  mail:
    host: smtp.163.com
    port: 587
    username: appleserver2026@163.com
    password: MVwkP6RGaHiizGk3
    protocol: smtp
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          ssl:
            enable: true

  # File Upload Configuration
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

# File Storage Configuration
file:
  storage:
    dir-a: ..\storage\INPUT
    dir-b: ..\storage\OUTPUT

# JWT Configuration
jwt:
  secret: your-256-bit-secret-key-for-jwt-signing-must-be-at-least-32-characters-long
  expiration: 86400000

# Logging
logging:
  level:
    com.filemanage: DEBUG
    org.springframework.security: DEBUG